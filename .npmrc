# Registry configuration for deterministic installs
# Agents and CI must be able to install without manual intervention
registry=https://registry.npmjs.org/

# Auth token for publishing is injected by setup-node action (NODE_AUTH_TOKEN)
# in the release workflow — not hardcoded here to avoid install-time warnings.

# Strict peer dependencies — surface breakages early
strict-peer-dependencies=false

# Ensure lockfile is respected in CI
# (pnpm install --frozen-lockfile is enforced in CI workflow)

# Hoist patterns for Nuxt/Vue compatibility
shamefully-hoist=true
